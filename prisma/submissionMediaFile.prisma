model SubmissionMediaFile {
  file         String     @unique
  path         String     @unique
  uploadedAt   DateTime   @default(now()) @map("uploaded_at")
  submissionId Int        @map("submission_id")
  submission   Submission @relation(fields: [submissionId], references: [id])
}
